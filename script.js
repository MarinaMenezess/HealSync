document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("cadastro");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./index.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("registro");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./registers.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("registrar");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./register.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("notificacao");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./notifications.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("configuracao");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./settings.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("mensagem");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./chat.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("logout");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./login.html";
    });
});

document.addEventListener("DOMContentLoaded", function () {
    // Seleciona o botão pelo ID
    var botao = document.getElementById("login");
    
    // Adiciona um evento de clique ao botão
    botao.addEventListener("click", function () {
        // Redireciona para a página desejada
        window.location.href = "./index.html";
    });
});